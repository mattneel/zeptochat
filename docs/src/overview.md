# Overview

Zeptochat is a minimal transformer training stack written in Zig 0.15.1. The project emphasizes CPU-first execution, straightforward SIMD acceleration, and an eventual WASM port. Consult `SPEC.md` for the full implementation blueprint and `TODO.md` for the active roadmap.
