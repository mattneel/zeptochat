# Overview

Zeptochat is a minimal transformer training stack written in Zig 0.15.1. The project emphasizes CPU-first execution, straightforward SIMD acceleration, and an eventual WASM port. Work happens on descriptive feature branches that merge back to `master` via pull request; consult `SPEC.md` for the full implementation blueprint and `TODO.md` for the active roadmap.
